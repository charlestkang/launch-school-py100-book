EXERCISE 1
What happens when you run the following program? Why do we get that result?

Copy Code
def set_foo():
    foo = 'bar'

set_foo()
print(foo)


ANSWER:

Because foo is initialized/assigned within the function and is limited to the function scope, the attempt
to reference foo from outside the function runs into a NameError since foo is not defined